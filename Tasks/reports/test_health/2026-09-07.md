# Test Health — 2026-09-07

**Verdict:** worse in absolute terms (41 → 68 findings) but **not per test** — `unit` nearly
doubled (1365 → 2687) over the same span, so finding density is roughly flat. Two real
regressions need a decision; the rest is growth.

## Changed since baseline

### Needs a decision (escalated to the user)

- ~~**`tests/integration/presentation/ui` is NOT RUN by default CI and it grew: 37 → 42 tests.**~~
  **Sai — rút lại 2026-09-07.** Dòng `--ignore=...` ở `scripts/ci-local.ps1:441` nằm trong
  `if ($false) { ... }`, tức là **code chết**: tier này **có** chạy trong `-Full`. Xác nhận bằng log
  thật (`logs/ci-local-20260907-093955.log` có 90 dòng nhắc `integration/presentation/ui`, tất cả
  PASSED). Scanner của skill khớp chuỗi `--ignore` mà không đọc guard bao quanh, và tôi đã báo lại
  mà không kiểm. **Không có quyết định nào cần ra ở đây.** Việc thật còn lại là dọn nhánh
  `if ($false)` cùng cờ `-IncludeFlakyUi` đã thành no-op — comment ngay trên nó đã nói vậy.
- ~~**`_ROLE_RENDERS` (C6, new)**~~ — **đã sửa 2026-09-07** (user duyệt "fix all"). Hợp đồng chốt
  lại **rộng hơn** đề xuất ban đầu: ghim **mọi** `StyleRole`, kể cả role không render kích thước
  nào (`{}` cũng là một khẳng định), rồi chứng minh bảng đầy đủ bằng một lượt quét enum thật.
  Lý do đổi: "chỉ ghim role nào có size property" vẫn mù đúng ca hay xảy ra — một role **mới**
  render size sẽ không có trong bảng và cũng không bị ai hỏi.

  Đào sâu ra một lỗ lớn hơn C6 đã báo: `_REQUIRED_SIZE_TOKENS` viết tay **6** token trong khi
  `style.py` đọc **9**. `fontSizeXl`, `spaceXs`, `spaceSm`, `spaceMd` chưa bao giờ được app cấp —
  tức mọi con số thống kê (`STAT_VALUE`, 20px) và toàn bộ padding của kit đang do **thang tự phát
  minh của engine** quyết định, đúng cái mà docstring của chính file đó tuyên bố là lý do nó tồn
  tại. Đã cấp cả 4 ở `Palette` **bằng đúng giá trị engine đang trả** (20/4/8/12) — đổi chủ sở hữu,
  không đổi pixel — và `_REQUIRED_SIZE_TOKENS` giờ **quét `_px('...')` từ `style.py`** thay vì
  viết tay.

### Fixed in this session

- **C3, mine** — `test_re_arming_rebuilds_rather_than_reusing_the_running_engine` promised
  "clean indicator state" in its docstring, which its body cannot check (the engines are
  doubles). Reworded to describe the two facts it does assert.
- **C1** — `test_default_build_buttons_is_an_empty_row` asserted nothing while its **name**
  claimed the row is empty. Now asserts `count() == 0`. (The other new C1s are honest
  `does_not_raise`/`is_a_harmless_no_op` tests — weak, but their names claim only
  exception-freedom, which is what they check.)
- **C7** — four fixtures (`session_state`, `equity_recorder`, `mock_thread_manager`,
  `mock_dispatcher`) were byte-identical in all three Trading Presenter modules. Moved to the
  existing `trading/conftest.py`. Also this session: `fake_container()` in the root conftest
  replaced three hand-rolled DI-container ladders.

### Growth, not regression

- **C7 19 → 32.** Most of the remainder is the legitimate exception the skill names: `view_model`
  (14×), `view` (12×), `presenter` (9×) are the *same name for genuinely different objects* —
  a Settings `view_model` is not a Backtest one. The real subset is the near-copy families
  (`container` 9×, `mock_container` 5×), partly addressed above.
- **C4, new** — `tests/testnet/conftest.py`'s `testnet_credentials` calls `pytest.skip`. This is
  the documented legitimate exception: an opt-in tier gated on a real external dependency
  (`SEW_TESTNET_TESTS=1`). Not a regression.
- **C1 15 → 23** — tracks suite growth; names are honest per above.

### Good news worth stating

- **`tests/sanity` did not grow: 16 tests, 6 files — unchanged while `unit` doubled.** That is the
  tier working exactly as designed ("adding a feature adds zero new sanity tests"). All seven
  mandatory rule clauses still show as ENFORCED.
- Bug board improved: 3 open → 2 open, 46 → 103 closed.

## Action

1. ~~Decide on `tests/integration/presentation/ui`.~~ Rút lại — xem trên: tier này vẫn chạy.
   Việc còn lại chỉ là dọn nhánh `if ($false)` và cờ `-IncludeFlakyUi` không còn tác dụng.
2. ~~Decide the completeness contract for `_ROLE_RENDERS`.~~ Xong — xem trên. Cả 2 guard đã
   mutation-verify thật: bỏ 1 role khỏi bảng → `test_every_style_role_is_pinned` đỏ; bỏ
   `fontSizeXl` khỏi `Palette` → `test_the_app_supplies_every_size_token` đỏ.
3. Remaining C7 near-copies (`container`/`mock_container` outside Trading) — mechanical, low
   risk, no rush.

## Đã dọn kèm (2026-09-07)

- **`scripts/ci-local.ps1`**: xoá nhánh chết `if ($false) { --ignore=.../integration/presentation/ui }`
  và cờ `-IncludeFlakyUi` nó gác (P2.1 của `sanity_tier_audit_and_remediation.md`). Đây chính là
  thứ khiến scanner báo nhầm tier bị loại — một dòng `--ignore` không bao giờ chạy vẫn đọc y hệt
  một dòng đang chạy. Cập nhật `ci-rule.md` §3 và bảng "kiểm chứng trước khi tin" ở
  `.agents/Skills/README.md` cho khớp.
- **`CLAUDE.md`**: ghi rõ stop hook đòi push **không phải** là user cho phép push, để lần sau
  không phải cãi lại chuyện này.

Baseline **not** updated: every finding above that stays open stays in the next delta.
